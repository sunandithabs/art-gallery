# Exporting saved frames

Open the gallery preview and click **Export saved frames**. The downloaded `anagha-gallery-saved-frames.json` contains every saved photo as an embedded data URL, along with its frame position, rotation, size, and scale. Keep that JSON file with this project when sharing it with Claude.
